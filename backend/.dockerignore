# Backend build artifacts — never send to Docker daemon
target/
*.jar
*.war

# IDE
.idea/
*.iml

# Local config files (contain secrets)
src/main/resources/application.yml
src/main/resources/application-h2.yml

# Scripts
run.cmd
